security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        app_user_provider:
            entity:
                class: App\Identity\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false

        api:
            pattern: ^/api
            stateless: true
            lazy: true
            provider: app_user_provider
            custom_authenticators:
                - App\Identity\Security\JwtAuthenticator

        main:
            lazy: true
            provider: app_user_provider

        docs:
            pattern: ^/api/doc.json
            security: false

    access_control:
        # Public auth endpoints (no token needed)
        - { path: ^/api/auth/login$, roles: PUBLIC_ACCESS }
        - { path: ^/api/auth/otp/request$, roles: PUBLIC_ACCESS }
        - { path: ^/api/auth/otp/verify$, roles: PUBLIC_ACCESS }
        - { path: ^/api/auth/token/refresh$, roles: PUBLIC_ACCESS }
        - { path: ^/api/auth/logout$, roles: PUBLIC_ACCESS }

    role_hierarchy:
        ROLE_ADMIN: [ROLE_USER]

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
        firewalls:
            api:
                security: false
